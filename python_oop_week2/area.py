print("โปรแกรมหาพื้นที่ 4 เหลี่ยมจัตุรัส")
a = int(input('ใส่ค่าด้าน'))
aa = int(input('ใส่ค่าด้าน'))
aaa =a * a
print(f'ค่าพื้นที่ของ 4 เหลี่ยมจัตุรัส = {aaa}')

print("โปรแกรมหาพื้นที่ 4 เหลี่ยมผืนผ้า")
b= int(input('ใส่ค่ากว้าง'))
bb = int(input('ใส่ค่ายาว'))
bbb =b * bb
print(f'ค่าพื้นที่ของ 4 เหลี่ยมจัตุรัส = {bbb}')

print("โปรแกรมหาพื้นที่ วงกลม")
c = float(input('ใส่รัศมีวงกลม'))
r = 3.14*c**2
print(f'ค่าพื้นที่ของพื้นที่ 4 เหลี่ยมจัตุรัส = {r}')